spring:
  application:
    name: media-service
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 129.211.26.164:8848
      username: nacos  # 也可以放在顶层
      password: nacos
      discovery:
        namespace: 61728b3c-2f54-4912-99d4-b7b283fad8f7
        group: media-service
      config:
        namespace: 61728b3c-2f54-4912-99d4-b7b283fad8f7
        group: media-service
        file-extension: yaml
        refresh-enabled: true
        shared-configs:
          - data-id: mybatis-plus-${spring.profiles.active}.yaml
            group: common-module
            refresh: true
  config:
    import: nacos:media-service-${spring.profiles.active}.yaml